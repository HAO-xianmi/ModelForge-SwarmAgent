"""Persistence layer: database, repositories, artifact registry, object store.

This is part of the core foundation (per the project owner): the Artifact
Registry and run-state versioning live here, and everything else uses them
rather than inventing parallel state stores.
"""
