"""ModelForge-Swarm benchmark suite (extension, not a separate product).

Offline harness that scores papers with the CompetitionJudge to (a) calibrate
and validate the rubric on a labeled real-paper corpus, and (b) gate workflow
changes: no architecture change is accepted unless benchmark scores rise across
the four problem categories.
"""
